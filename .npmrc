; Tempo Accounts SDK (`accounts`) declares optional peer deps (Privy, Expo,
; React Native) that crash npm's dependency resolver ("edgesOut" error) on
; default installs. Legacy peer-dep resolution avoids the crash and does not
; pull in those unused optional peers. Required for `npm install` to succeed.
legacy-peer-deps=true
